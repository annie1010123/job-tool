# JobPilot 開發復盤 — 2026-06-10

## 這次做了什麼

這次對話橫跨三條主線：UI 調整、爬蟲修復、推薦系統診斷。

---

## 主線一：UI 調整

### 做了什麼
- **收藏區 SavedJobList**：來源篩選器改成 104/Cake/LinkedIn/Yourator/手動新增；白色卡片整體可點擊（onClick 跳 /board/:id）；按鈕改名「投遞準備」
- **背景色統一**：`/board` 和 `/board/[id]` 從 `bg-zinc-50` 改成 `style={{ background: "#f1efe8" }}`，跟 Dashboard 一致
- **求職意圖頁 mockup**：分析現有 UX 問題，做出 A/B/C/D 四個狀態的改版 mockup（步驟指示器、spinner、範例 chips、成功反饋畫面）

### 遇到的問題
- **JD 描述消失**：點進收藏卡片發現 description 全空。根本原因是舊爬蟲流程「搜尋→存基本資料」從未打 detail API，所以 551 筆 JD 裡 493 筆 description 是 null。
- **手動 patch 被 Cloudflare 擋**：用 Playwright 補抓兩筆用戶職缺的描述時，其中一筆（昇恆昌）遇到 Cloudflare challenge，DOM scraping 失敗。

### 怎麼解
- 先用 `patch-user-jds.ts` 對用戶已收藏的兩筆職缺做 DOM scraping（`div.job-description`），成功補上 950 字描述
- 大規模 backfill 問題留待爬蟲修復後一併解決

---

## 主線二：爬蟲修復（核心瓶頸）

### 問題根源
透過 `debug-search.ts` 確認：爬蟲自 2026-05-28 起完全失效。

**原因**：104 啟用 Cloudflare Bot Management，對 headless Chromium 進行 TLS fingerprint 偵測。Playwright 的 JA3/JA4 指紋跟真實瀏覽器差異太大，搜尋頁直接回傳 307 → 403，`waitForResponse('/jobs/search/list')` 永遠 timeout，circuit breaker 在第五個關鍵字就觸發。

### 解法選擇
放棄 Playwright，改用 **Python + curl_cffi**。

curl_cffi 底層用 libcurl，可以完整偽裝 Chrome 的 TLS 指紋（JA3/JA4 + ALPN + cipher suites），對 Cloudflare 來說跟真實 Chrome 無法區分。

### 實作了什麼（`worker/crawler/run.py`）
依照 9 點反爬蟲規格全部實作：

| 機制 | 實作方式 |
|---|---|
| TLS 指紋偽裝 | `curl_cffi.Session(impersonate="chrome124")` |
| UA 池 + session 粘性 | 開頭隨機選一個 UA，整個 session 固定 |
| 動態 Referer | 搜尋 API 用搜尋頁 URL，detail API 用職缺頁 URL |
| 隨機延遲 | 0.5–1.5s + 5% 機率多等 3–10s |
| 小時配額 | 自維護 timestamps list，超過 1500 筆就等 |
| 429 指數退避 | 30 × 2^n 秒，最久 240s |
| Circuit breaker | 連續 5 次錯誤直接停 |
| 兩段式流程 | 先打 list API 拿 jobNo，再打 detail API |
| 翻頁早停 | order=12 降序，整頁 appearDate 早於 cutoff 就停 |

### 驗收結果
首次執行：9 筆新職缺，失敗 0 筆，全部有 description（最長 1015 字）。
**關鍵差異**：舊爬蟲用 Playwright 打 detail API 拿回的 `jobDescription` 是 null（需要真實 browser session）；curl_cffi 偽裝成真實 Chrome 後，API 直接回傳完整內文。

---

## 主線三：推薦系統診斷

### 現況掌握
推薦分數算法：
```
finalScore = 0.5 × intentScore + 0.5 × keywordScore
           × recencyBoost  (今天/昨天 +15%)
           × competitionBoost (應徵人數 < 10 +10%)
```
- `intentScore`：求職意圖 embedding vs JD embedding 的 cosine 相似度（pgvector）
- `keywordScore`：關鍵字在 title + description 的覆蓋率

cron 每天推 10 筆：7 天去重、每公司最多 1 筆、最後按薪資排序。

### 發現的問題

**問題一（最嚴重）：新爬職缺永遠不會被推薦**

推薦 SQL 是 `INNER JOIN JdEmbedding`，沒 embedding 的 JD 直接不進查詢。
Python 爬蟲沒有 embedding API key → `embed_jd()` 永遠回傳 `[]` → embedding 從未寫入。
今天爬的 11 筆：全部 ❌ embedding。

```
總 JD: 551
有 embedding: 502  ← 推薦池
今天新爬有 embedding: 0 ← 爬了白爬
```

**問題二：明天沒新職缺會怎樣？**

會推，但從舊池撈。502 筆每天推 10 筆，理論上可撐 ~50 天，但池子不會增長。

**問題三：關鍵字是靜態的**

onboarding 設定後就不變，不會根據用戶行為（收藏、投遞）自動更新。

### 優化假設

| 優先 | 方向 | 說明 |
|---|---|---|
| 高 | Gemini embedding | `.env.local` 已有 `GEMINI_API_KEY`，`text-embedding-004` 輸出 768 維，與 schema 相容，免費。在爬蟲裡補上即可讓新職缺進推薦池 |
| 中 | Keyword fallback | 對沒有 embedding 的 JD 用純關鍵字比對作為第二層候選，不完全依賴 embedding |
| 低 | 行為反饋 | 收藏/投遞的職缺技能欄自動回饋進關鍵字，讓意圖隨使用進化 |

---

## 技術債清單

1. **493 筆 JD 沒有 description**：需要 backfill，但要繞過 Cloudflare（Python 爬蟲可以做）
2. **新 JD 沒有 embedding**：最優先，接 Gemini API 就解
3. **求職意圖頁 UX**：mockup 做好了，還沒實作（改一行 `setSelected(new Set())` + 其他 UI 改動）
4. **Keywords 靜態問題**：中長期，暫時不急

---

## 架構備忘

```
104 Search API:  GET /jobs/search/api/jobs?ro=0&kwop=7&keyword={kw}&area=6001001000%2C6001002000&order=12&page={n}
104 Detail API:  GET /job/ajax/content/{jobNo}
DB:              Supabase PostgreSQL，ORM Prisma，prisma db push（不用 migrate dev）
Embedding:       pgvector 768 維，表 JdEmbedding
推薦觸發:        GET /api/cron/daily（Bearer CRON_SECRET）
爬蟲:            python3 worker/crawler/run.py（讀 .env.local DIRECT_URL）
```
